# MySpy Cursor 🕵️‍♂️

**MySpy Cursor** — MCP сервер для поиска, анализа и фильтрации рекламы в Facebook прямо из вашего редактора кода (Cursor или Claude Code). Он использует  **Gemini AI**, чтобы анализировать содержимое рекламы (картинки/видео) и отсеивать мусор (например, курсы, книги и дропшиппинг).

## ✨ Возможности
- **Поиск рекламы в Facebook**: Находите активные объявления по ключевым словам и странам.
- **Умная фильтрация**: Искуственный интеллект отсеивает нерелевантные объявления (обучающие курсы, инфобизнес и т.д.) и оставляет нутру.
- **Анализ медиа**: Gemini автоматически описывает, что происходит на картинках и видео.
- **Интеграция с Cursor**: Общайтесь с базой рекламных объявлений просто спрашивая чат-бота в Cursor или Claude Code.

---

## 🚀 Установить можно двумя способами

### Вариант 1: если вы не технарь и не пользуетесь терминалом.

1. **Скачайте проект**: Нажмите зеленую кнопку **Code** (вверху страницы) -> **Download ZIP**.
2. **Распакуйте** архив в любую папку.
3. **Запустите `install_windows.bat`**.
   - Скрипт сам проверит наличие Python и установит библиотеки.
   - *Если увидите ошибку "Python not found"*: Скачайте его с (https://www.python.org/downloads/) (не забудьте галочку **Add to PATH** при установке - это важно!) и запустите скрипт снова.

### Вариант 2: Продвинутый (Ручная установка)
Если вы хотите полный контроль или у вас MacOS/Linux.

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/whygithub7/MySpy-Cursor.git
   cd MySpy-Cursor
   ```
2. Создайте и активируйте виртуальное окружение:
   ```bash
   # Windows
   python -m venv venv
   .\venv\Scripts\activate

   # Mac / Linux
   python3 -m venv venv
   source venv/bin/activate
   ```
3. Установите зависимости:
   ```bash
   pip install -r facebook-ads-library-mcp/requirements.txt
   ```
4. Создайте файл настроек:
   ```bash
   # Windows
   copy .env.template .env
   
   # Mac / Linux
   cp .env.template .env
   ```

### Настройка ключей
1. После установки в папке появится файл с именем `.env`. Откройте его с помощью обычного Блокнота.
2. Вам понадобятся два ключа:
   - **SCRAPECREATORS_API_KEY**: Взять на сайте (https://scrapecreators.com/). Нужен для поиска самой рекламы.
   - **GEMINI_API_KEY**: Взять в (https://aistudio.google.com/app/apikey). Нужен для умной фильтрации.
3. Вставьте ваши ключи после знака равно `=`, без пробелов.
   Пример:
   ```
   SCRAPECREATORS_API_KEY=ваш_длинный_ключ_здесь
   GEMINI_API_KEY=ваш_ключ_гугл_здесь
   ```
4. Сохраните файл и закройте его.

---

## 🤖 Как подключить к Cursor

1. Откройте редактор **Cursor**.
2. Зайдите в Настройки ⚙️ -> **Tools & MCP ** -> **New MCP Server**.
3. Попросите агента заполнить по образцу:

{
  "mcpServers": {
    "fb_ad_library": {
      "command": "C:\\Полный\\Путь\\К\\Папке\\MySpy-Cursor\\venv\\Scripts\\python.exe",
      "args": [
        "C:\\Полный\\Путь\\К\\Папке\\MySpy-Cursor\\facebook-ads-library-mcp\\mcp_server.py"
      ]
    }
  }
}


5. Копипаст и **Save**.

---

## 🛠 Использование

### Вы можете просто писать в чат:
> "Найди рекламу по запросу 'похудение' в Германии и проанализируй картинки."
> "Покажи мне объявления про 'кето диету', но убери курсы и книги."

### Скрипт поиска (`search_ads.py`)
служит просто площадкой для тестирования и хранилищем нового функционала.  
---

## 📋 Требования
- Python 3.10 или новее
- Рабочий ключ ScrapeCreators
- Ключ Google Gemini
